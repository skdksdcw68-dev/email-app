source "https://rubygems.org"

# Pinned for reproducible CI. Unpinned, every run installs whatever fastlane
# shipped that day, so signing can break on a commit that touched nothing.
# 2.237.0 is the version already proven on the remi pipeline.
gem "fastlane", "2.237.0"
